# Copy to .env and fill in. Do not commit .env.
# Get the service role key from: Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Airtable — for 10k compound sync (scripts/airtableSync.ts, scripts/seedIngestQueue.ts)
# Create a Personal Access Token at https://airtable.com/create/tokens
AIRTABLE_ACCESS_TOKEN=pat...
AIRTABLE_BASE_ID=app...

# Optional: for AI chat and richer PK extraction. Omit for free operation (regex fallback for PK; chat still fetches compounds).
# OPENAI_API_KEY=sk-...
